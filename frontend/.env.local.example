# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Internal secret for cookie signing (generate with: openssl rand -base64 32)
SESSION_SECRET=your-session-secret-here-change-in-production

# Cookie settings
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost
